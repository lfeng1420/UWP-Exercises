<Page
    x:Class="GoNutsPrep.SchedulePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GoNutsPrep"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="Green">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <Image Grid.Row="0" Grid.ColumnSpan="2" Source="Assets/white-logo.png" Style="{StaticResource LogoStyle}" />
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Pickup:" Foreground="White" FontSize="18" Margin="20,20,20,0"/>
        <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Vertical" Margin="20,20,20,0">
            <CalendarDatePicker PlaceholderText="select a date" Margin="0,0,0,20"/>
            <TimePicker Foreground="White"/>
        </StackPanel>

    </Grid>
</Page>
